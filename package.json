{
  "name": "simple-ui",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w client\" \"npm run dev -w server\" --names \"client,server\" --prefix-colors \"cyan,yellow\"",
    "build": "npm run build -w shared && npm run build -w client && npm run build -w server",
    "migrate": "npm run migrate -w server",
    "migrate:rollback": "npm run migrate:rollback -w server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
